package partial

import "testing"

// TestPartialBindDemo runs the example end-to-end. The function panics on
// any post-condition failure, so a panic surfaces as a test failure.
func TestPartialBindDemo(t *testing.T) {
	PartialBindDemo()
}
